apiVersion: v1
kind: Service
metadata:
  name: {{ include "cpp_streamer.fullname" . }}
  labels:
    {{- include "cpp_streamer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cpp_streamer.selectorLabels" . | nindent 4 }}
