apiVersion: v1
kind: Service
metadata:
  name: {{ include "cpp-streamer.fullname" . }}
  labels:
    {{- include "cpp-streamer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cpp-streamer.selectorLabels" . | nindent 4 }}
