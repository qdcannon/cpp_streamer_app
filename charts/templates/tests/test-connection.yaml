apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cpp-streamer.fullname" . }}-test-connection"
  labels:
    {{- include "cpp-streamer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cpp-streamer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
